from random import randint

mapping = dict()
encoded_message = '111'
count=0
string = ""

for i in range(1, 27):
    mapping[str(i)] = chr(96+i) 
#print(mapping)
#print(encoded_message)

for i in range(3):
    for c in encoded_message:
        if not count:
            string = string + mapping[c].value
        elif mapping[]+mapping:

            if :
    count += 1